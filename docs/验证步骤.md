当前的项目 按步骤验证 本地docker启动，除了数据库和监控服务的服务一直运行，其他服务依次验证，验证成功后为节省内存即可关闭，最后关闭数据库服务，
尽量在docker中进行打包构建，避免在本地环境的依赖问题
1. 正常启动PG数据库，
2. 正常执行数据生成脚本，确认数据生成成功
3. 正常启动监控服务，确认监控服务正常成功，能正常访问监控服务，监控服务能正常链接其他服务
3. 依次打包构建所有后端服务，确认服务启动成功，docker启动成功，服务端口正常，能正常访问数据库，能正常访问监控服务，
4. 打包构建前端服务，正常访问前端服务，确认前端服务正常运行
